<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorMainHeadings</item>
        <item name="colorPrimaryDark">@color/colorMainHeadings</item>
        <item name="colorAccent">@color/colorHighlight</item>
    </style>

    <style name="Autocomplete">
        <item name="android:textColor">@color/colorMainHeadings</item>
        <item name="colorControlNormal">@color/colorMainHeadings</item>
        <item name="colorControlActivated">@color/colorHighlight</item>
    </style>

</resources>
